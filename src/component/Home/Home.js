import React from 'react';
import { Button } from 'react-bootstrap';

const Home = () => {
    return (
        <div>
            <h1>Hello</h1>
            <Button variant="primary">Primary</Button>
        </div>
    );
};

export default Home;